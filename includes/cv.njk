---
layout: base.njk
---
<header class="page-header">
    <h1 class="cv-header page__header-row">
        <span class="cv-header__name">{{ resume.name }}</span>, <span class="cv-header__title">{{ resume.title }}</span>
    </h1>

    {% set contacts = resume.contacts %}
    {% if contacts %}
        <ul class="cv-contacts page__header-row">
            {% for contact in contacts %}
                <li class="cv-contacts__contact"><strong class="cv-contacts__name">{{ contact.name }}</strong>:
                    <a class="cv-contacts__link"
                       href="{{ contact.link }}" {% if contact.external %} target="_blank" {% endif %}>
                        {{ contact.value }}
                    </a>
                </li>
            {% endfor %}
        </ul>
    {% endif %}

    {% set status = resume.status %}
    {% if status %}
        <p class="cv-status page__header-row">{{ status }}</p>
    {% endif %}
</header>
<main>
    {% set exps = resume.summary %}
    {% if exps %}
        <section>
            {% include './_cv-exp.njk' %}
        </section>
    {% endif %}

    {% set exps = resume.last_exp %}
    {% if exps %}
        <section>
            {% include './_cv-exp.njk' %}
        </section>
    {% endif %}

    {% set exps = resume.early_exp %}
    {% if exps %}
        <section class="cv-hr">
            {% if resume.early_exp_title %}
                <h2 class="cv-section-header cv-hr__header">{{ resume.early_exp_title }}</h2>
            {% endif %}
            {% include './_cv-exp.njk' %}
        </section>
    {% endif %}

    {% set exps = resume.ed_exp %}
    {% if exps %}
        <section class="cv-hr">
            {% if resume.ed_exp_title %}
                <h2 class="cv-section-header cv-hr__header">{{ resume.ed_exp_title }}</h2>
            {% endif %}
            {% include './_cv-exp.njk' %}
        </section>
    {% endif %}
</main>
